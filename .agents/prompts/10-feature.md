# Feature or fix

Implement the smallest complete change. Reproduce bugs with the narrowest unit test or named GUI scenario. Run the changed-scope verifier once after the edit batch is stable.

Ordinary product work does not run formal AI review, install the app, or perform Computer Use. Those occur only for mapped system boundaries or an explicit request. With explicit delivery authorization, push one atomic commit directly to `dev`, then remove and prune the task worktree.
