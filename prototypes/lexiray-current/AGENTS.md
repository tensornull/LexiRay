# LexiRay Current Product Prototype

- This folder is an isolated design artifact. It must not join the Swift/Xcode build or become production application code.
- Use mock data only. Never access provider credentials, real history, user defaults, or `~/.lexiray`.
- Treat the current fingerprint-bound acceptance screenshots under `build/ui-artifacts/` as the visual source of truth.
- Match the existing dense, calm, native macOS design language before proposing improvements.
- Keep Dashboard, Providers, Settings, Floating Panel, and embedded History behavior interactive.
- Run and visually inspect the prototype in the in-app browser before handoff.
